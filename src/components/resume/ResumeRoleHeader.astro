---
interface Props {
  company: string;
  title: string;
  dates: string;
  /** Tertiary note after the title, e.g. promotion date. */
  promotion?: string;
  /** Secondary lines under company · title (prior roles, acquisition notes). */
  meta?: readonly string[];
}

const { company, title, dates, promotion, meta } = Astro.props;
---

<p class="resume-role-header text-heading">
  <span class="resume-role-company">{company}</span>
  <span class="resume-role-sep" aria-hidden="true">·</span>
  <span>{title}</span>
  {promotion && <span class="resume-role-promotion text-body">{promotion}</span>}
  <span class="resume-role-dates text-body">{dates}</span>
</p>
{
  meta && meta.length > 0 && (
    <div class="resume-role-meta text-body">
      {meta.map((line) => (
        <p>{line}</p>
      ))}
    </div>
  )
}
