import {
  BLOCKS,
  BLOCKS_SPAWN,
  BLOCKS_SPAWN_MOBILE,
  STATIC_TILE_POSES,
  type BlockDef,
} from "./physicsBlocks354";

export type TileDef = BlockDef;

export const TILES = BLOCKS;
export const TILES_SPAWN = BLOCKS_SPAWN;
export const TILES_SPAWN_MOBILE = BLOCKS_SPAWN_MOBILE;
export { STATIC_TILE_POSES };
